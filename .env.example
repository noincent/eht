# Environment Variables for Employee Handbook Tool

# API Keys - Get your API key from https://console.anthropic.com/
ANTHROPIC_API_KEY=your-api-key-here

# LLM Configuration
MAX_TOKENS=1500            # Maximum response tokens
TEMPERATURE=0.1            # Lower value for more deterministic responses
LLM_MODEL=latest           # Anthropic model to use

# Handbook Paths - Replace with your actual handbook file paths
HANDBOOK_PATH_ZH=employee_handbook_zh.docx    # Path to Chinese handbook (DOCX)
HANDBOOK_PATH_EN=employee_handbook_en.docx    # Path to English handbook (DOCX)
HANDBOOK_PATH_ZH_PDF=employee_handbook_zh.pdf # Path to Chinese handbook (PDF) - Optional
HANDBOOK_PATH_EN_PDF=employee_handbook_en.pdf # Path to English handbook (PDF) - Optional

# System Directories - Default values shown, can be modified if needed
INDEX_PATH=./indexes       # Directory for storing search indexes
CACHE_DIR=./llm_cache      # Directory for caching LLM responses
FEEDBACK_DIR=./feedback    # Directory for storing user feedback
LOG_DIR=./logs             # Directory for application logs

# Web Application
PORT=8007                  # Web application port number